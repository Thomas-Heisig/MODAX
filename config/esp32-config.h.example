// MODAX ESP32 Field Layer Configuration
// Copy relevant parts to esp32-field-layer/src/main.cpp

// WiFi Configuration
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// MQTT Broker Configuration
#define MQTT_SERVER "192.168.1.100"  // IP address of MQTT broker
#define MQTT_PORT 1883
#define MQTT_USERNAME ""             // Leave empty if no authentication
#define MQTT_PASSWORD ""             // Leave empty if no authentication

// Device Configuration
#define DEVICE_ID "ESP32_FIELD_001"  // Unique identifier for this device

// Sensor Configuration
#define CURRENT_SENSOR_PIN_1 34      // ADC pin for current sensor 1
#define CURRENT_SENSOR_PIN_2 35      // ADC pin for current sensor 2
#define TEMP_SENSOR_PIN_1 32         // ADC pin for temperature sensor
#define EMERGENCY_STOP_PIN 25        // Digital input for emergency stop
#define DOOR_SENSOR_PIN 26           // Digital input for door sensor

// Timing Configuration
#define SENSOR_INTERVAL 100          // Sensor reading interval (ms) - 10Hz
#define SAFETY_INTERVAL 50           // Safety check interval (ms) - 20Hz

// Safety Thresholds
#define CURRENT_OVERLOAD_THRESHOLD 10.0  // Amperes
#define TEMPERATURE_MAX_THRESHOLD 85.0   // Celsius

// MQTT Topics
#define TOPIC_SENSOR_DATA "modax/sensor/data"
#define TOPIC_SAFETY "modax/sensor/safety"
